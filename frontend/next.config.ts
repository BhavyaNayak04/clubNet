import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "images.squarespace-cdn.com",
      "images.pexels.com",
      "www.bookbollywoodsingers.com",
      "img.freepik.com",
    ],
  },
};

export default nextConfig;
